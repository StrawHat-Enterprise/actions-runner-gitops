# Community ARC Controller (actions-runner-controller by summerwind)
# Note: Don't set namespace globally - each resource specifies its own namespace
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - external-secret.yaml
  - helmrelease.yaml
